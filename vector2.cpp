#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <sstream>

#include <algorithm>
using namespace std;

int main()
{
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    vector<int> arr;
    vector<int> arr1;
    int n;
    cin >> n;
    arr.push_back(n);
    for (int i = 0; i < n; i++)
    {
        int k;
        cin >> k;
        arr[i] = arr
    }

    return 0;
}
